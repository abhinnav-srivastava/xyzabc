# Docs — AI context

For full project context (structure, conventions, key paths), see **repository root `CLAUDE.md`**.

This folder contains:

- **CODE_METRICS_AND_TOOLS.md** — Patch-level and project-level tools (Git, CLOC, diffstat, pygount, radon; future Detekt, Android Lint, etc.), install/bundle notes.
- **PORTABLE_AND_ELECTRON.md** — Portable Windows / Electron distribution: what to bundle (native exes only), no pip/Java/PATH at runtime.
